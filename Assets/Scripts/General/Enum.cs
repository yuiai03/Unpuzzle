public enum TileType
{
    Left,
    Right,
    Up,
    Down,

    Hide,
    Wall,
}

public enum PoolType
{
    Explosion,
}
public enum ToggleType
{
    BGM,
    SFX,
}
public enum AudioType
{
    Theme,
    ButtonClick,
    TileClick,
    Explosion,
}